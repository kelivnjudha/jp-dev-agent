# Jade Device Agent Security Skeleton

This scaffold starts the Jade Device Agent with the security model we want
later: user auth plus approved device proof. The executable is not the final
boundary. Jade-Palace-API must eventually reject POS-sensitive and checkpoint
requests that do not include valid approved device proof.

## Current Phase

JPDEVAGENT-R3-B-D-C wires the desktop Agent setup-code form to the real Branch
Device Registry claim endpoint and shows a safe pending-activation state.

- No automatic pending activation polling.
- No active device session challenge/sign/issue loop.
- No device session heartbeat loop.
- No POS forwarding.
- No printer bridge implementation.
- No NFC hardware integration.
- No Raspberry Pi kiosk mode.
- No production secrets.

The local mock activate/disable controls are hidden and inert unless
`JDA_ENABLE_MOCK_DEVICE_FLOW=true` is set for local development.

## Setup-Code-First Rule

The Agent must not create a device request without a setup code generated by an
authorized JP Admin user. This prevents random installer executions from
polluting branch device requests.

The setup code will be:

- single-use
- short-lived
- branch-bound
- capability-bound
- stored as a hash
- shown only once
- revocable before use
- audited

In the desktop Agent, the setup code is copied only into local submit scope,
sent once through the main process, and then cleared from the renderer input.
It is not logged, persisted, copied into pending state, or displayed after
submission.

## Device Identity

The scaffold creates an Ed25519 keypair through Node crypto. The private key is
never logged or displayed. Local file storage exists only as an isolated
development placeholder. Production must move private key handling to OS
keychain, encrypted storage, TPM-backed storage, or an equivalent protected
mechanism.

Hardware fingerprint is advisory. It is not authentication.

The API client maps the agent's local `publicKeyPem` value to the API's
`publicKey` request field for setup-code claim. Private key material is never
part of any device-side API DTO.

After a successful claim, the Agent persists only safe pending activation
metadata:

- device id
- server status
- branch summary
- allowed capabilities from the server
- safe HID prefix
- optional sanitized device label
- claim timestamp

The pending record must not contain setup codes, setup-code hashes, session
tokens, token hashes, private keys, public keys, full hardware fingerprint
hashes, or raw API bodies.

## Device API Client Rules

The device client is limited to these fixed endpoints:

- `POST /api/v1/branch-devices/claim`
- `POST /api/v1/branch-devices/session/challenge`
- `POST /api/v1/branch-devices/session`
- `POST /api/v1/branch-devices/heartbeat`

The client:

- builds URLs from `apiBaseUrl` plus fixed relative endpoint constants
- rejects embedded URL credentials
- does not accept arbitrary upstream paths from callers
- whitelists setup-code claim payload fields
- never accepts device-supplied capabilities
- validates successful responses before returning them
- maps HTTP/API errors to safe typed errors
- does not log setup codes, public keys, private keys, signatures, hardware
  fingerprint hashes, session tokens, or Authorization header values

The raw device session token is returned only from the session issue function.
It must be treated as memory-only sensitive data by later phases and must never
be exposed to the Electron renderer.

## Local Proxy Rules

The local proxy must:

- bind only to `127.0.0.1`
- reject unknown paths
- reject arbitrary upstream URLs
- avoid LAN binding
- never expose private key material
- log only safe metadata

Current endpoints:

- `GET /health`
- `GET /device/status`
- `POST /proxy/test`

`POST /proxy/test` is dev-only and does not forward to Jade-Palace-API.

R3-B-D-B does not add proxy forwarding. Future forwarding remains gated on an
active approved device session plus strict path/capability allowlists.

## Future Proxy Proof

Future signed requests should include:

- device id
- device session id/token
- timestamp
- nonce
- request body hash
- signature
- app/agent version

The API should validate freshness, replay protection, device status, branch
binding, and required capability.

## Electron Renderer Boundary

Renderer security defaults:

- `contextIsolation: true`
- `nodeIntegration: false`
- preload exposes a minimal bridge only
- renderer never receives private key material

The renderer can ask the main process for safe status, submit a setup-code
claim, and read placeholder hardware status. It does not receive private keys,
public keys, session tokens, full hardware fingerprint hashes, storage paths,
or a general API client.

## Future POS Enforcement Flow

Target flow:

1. User opens JPPOS inside the approved Agent shell.
2. JPPOS sends POS-sensitive requests to the local Agent proxy.
3. Agent attaches device proof.
4. API verifies user, POS session, device session, branch, and capability.
5. API rejects missing or invalid device proof.

Direct browser or direct API calls must fail once enforcement is enabled.

## Future Checkpoint Mode

The same registry/proxy/security model will support Raspberry Pi checkpoint
mode:

- QR display capability
- NFC reader capability
- heartbeat
- remote revoke/disable
- branch/checkpoint binding

Offline attendance acceptance is not part of the MVP.
